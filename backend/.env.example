# Base de datos Neon.tech
# Obtén estas credenciales desde el dashboard de Neon.tech: https://console.neon.tech
DB_HOST=ep-xxxxx-pooler.us-east-1.aws.neon.tech
DB_PORT=5432
DB_USERNAME=neondb_owner
DB_PASSWORD=tu_password_de_neon
DB_NAME=neondb

# JWT - Genera un secreto seguro para producción
# Puedes generar uno con: openssl rand -hex 64
JWT_SECRET=tu_jwt_secret_super_seguro_minimo_32_caracteres
JWT_EXPIRES_IN=7d

# Servidor
PORT=3000
NODE_ENV=development

# CORS - URL del frontend para permitir peticiones
CORS_ORIGIN=http://localhost:3001

# Frontend URL (para WebSocket y CORS)
# URL completa del frontend para conexiones WebSocket
FRONTEND_URL=http://localhost:3000